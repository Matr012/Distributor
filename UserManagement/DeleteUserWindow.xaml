<Window x:Class="MMZClient.UserManagement.DeleteUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Delete User" Height="260" Width="500"
        Background="Black" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Delete User" FontSize="18" FontWeight="SemiBold" 
               Foreground="Turquoise" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <Label Content="User ID:" Foreground="Turquoise" FontWeight="SemiBold"/>
            <TextBox x:Name="tbxUserId" Background="Black" Foreground="Turquoise" 
                     FontSize="16" Padding="4"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
            <Button x:Name="btnDelete" Content="Delete User" Click="btnDelete_Click" 
                    Margin="10" Padding="20,10" Background="DarkRed" Foreground="White" 
                    BorderBrush="Red" FontWeight="SemiBold"/>
            <Button x:Name="btnCancel" Content="Cancel" Click="btnCancel_Click" 
                    Margin="10" Padding="20,10" Background="Black" Foreground="Turquoise"/>
        </StackPanel>
    </Grid>
</Window>